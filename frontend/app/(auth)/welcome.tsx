import { StyleSheet, Text, View } from "react-native";

const welcome = () => {
  return (
    <View>
      <Text>Welcome</Text>
    </View>
  );
};

export default welcome;

const styles = StyleSheet.create({});
